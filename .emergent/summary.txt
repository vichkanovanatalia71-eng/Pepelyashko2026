<analysis>
The AI engineer's work primarily focused on expanding the application's document management capabilities. Initially, existing UI/UX was revamped to match  design, and a critical email sending bug was fixed by updating SMTP credentials. The core of recent work revolved around adapting new document templates (invoice, act, waybill, contract), integrating Google Drive for PDF storage, and enhancing document visibility within the counterparty interface. Challenges included Google Sheets API quota limitations, font rendering issues for PDF generation, and frontend state management complexities for dynamic forms. The latest effort involves creating a customizable contract template editor in the frontend and integrating it with the backend PDF generation logic, ensuring dynamic content and styling.
</analysis>

<product_requirements>
The application is designed for dynamic business document management. Key features include:
1.  **Counterparty Management**: Create and store counterparty data from a Google Sheet (Основні дані), with EDRPOU search and detailed views. Future plans include frontend editing.
2.  **Document Creation**:
    *   **Invoices, Acts, Waybills, Specifications**: Generate from user-provided templates with static supplier data (Мої дані) and dynamic buyer data (Основні дані). The app is не платник ПДВ (not a VAT payer).
    *   **Orders**: A dedicated section to create orders and generate documents.
    *   **Contracts**: PDF generation using a customizable HTML-like template, populating data from Основні дані and Мої дані. Functionality includes preview, email, download, and Google Drive storage (Shared Drive) with specific folder/naming conventions (Договори, document name + EDRPOU). Ukrainian characters must render correctly. Contract numbering: .
    *   **Document Numbering**: Invoice/Act/Waybill numbering: .
    *   **Document Viewing**: All generated documents (contracts, invoices, acts, waybills) should be viewable within the counterparty card, sorted by creation date (latest first), via Google Drive Viewer.
    *   **Template Customization**: Implement a frontend editor for document templates (starting with contracts) allowing text editing and variable insertion.
3.  **UI/UX**: Dynamic styling aligned with  (blue theme, yellow/orange accents, Montserrat font, rounded cards, clean forms).
</product_requirements>

<key_technical_concepts>
-   Full-stack: React (frontend), FastAPI (backend).
-   Data Storage: Google Sheets API.
-   Document Storage: Google Drive API.
-   PDF Generation: , , , .
-   Email Service: SMTP.
-   UI: Tailwind CSS, custom CSS,  icons.
-   Backend: Pydantic.
-   Excel parsing: .
</key_technical_concepts>

<code_architecture>
The application follows a standard full-stack architecture with a React frontend and a FastAPI backend.

-   ****:
    -   **Importance**: Core FastAPI application, defines all REST API endpoints.
    -   **Changes**: Expanded for orders and contracts.  model allows  as optional.  Pydantic model includes 11 fields. Added PUT endpoint for counterparty updates. Most recently, added new GET endpoints (, , , ) to fetch documents, which now return . Also, new POST endpoints for generating invoices, acts, and waybills were added (, , ). The contract generation endpoint () was updated to use  and now accepts a  field in . Syntax errors and unused imports were fixed.
-   ****:
    -   **Importance**: Encapsulates Google Sheets API logic.
    -   **Changes**: Fixed numerical data typing. Added , , and methods for document creation using Основні дані. Configured . A new method was added to retrieve supplier data from the Мої дані sheet. Crucially, the methods , , , , , , , and  were modified to store and retrieve  for all document types. It also includes  for fetching specific counterparty data.
-   ****:
    -   **Importance**: Generates PDF contracts and sends emails.
    -   **Changes**: Switched to  with DejaVu fonts for Ukrainian characters. Integrated with . Refactored  for HTML-like template and . This file is being superseded by  for new contract generation.
-   ** (NEW)**:
    -   **Importance**: A new file created to handle the generation of contracts using the detailed user-provided template.
    -   **Changes**: This file was created and progressively updated to incorporate the full contract text with placeholders for dynamic data from Мої дані, Основні дані, and Замовлення. It uses  for PDF generation. Initial style conflicts (e.g., 'Title' already defined) and font issues ( not found) were resolved by renaming styles and switching to available fonts. It's currently being updated to process a custom template passed from the frontend.
-   ****:
    -   **Importance**: Dedicated service for Google Drive API interactions.
    -   **Changes**: Handles service account authentication, folder creation, and file uploads, including Shared Drive support.
-   ****:
    -   **Importance**: Centralizes PDF generation logic for invoice, act, and waybill documents.
    -   **Changes**: File was created and fully implemented with PDF generation logic for these document types.
-   ****:
    -   **Importance**: Stores environment-specific variables.
    -   **Changes**: Updated with user-provided SMTP credentials (, ) to fix email sending.
-   ****:
    -   **Importance**: Python dependencies.
    -   **Changes**: Added , , .
-   ****:
    -   **Importance**: Main React component managing state, routing, and UI.
    -   **Changes**: Integrated Google Drive viewer. Updated counterparty detail card to display all 11 fields. Initial design updates via global CSS. New state variables were added (, , , , ). The  function was updated to call new API endpoints and open document previews. A new  was added. The counterparty card was updated to display lists of all generated documents (invoices, acts, waybills, contracts) with Переглянути (View) buttons, fetching . The contract creation form was enhanced to include an itemized list for goods/services. A Template Editor Dialog was added for contracts, allowing template editing with formatting options (font size, bold, italic, underline, alignment markers). Frontend state and API calls were updated to handle template saving and loading.  icon was imported for item removal.
-   ****:
    -   **Importance**: Provides custom CSS styles.
    -   **Changes**: Expanded with new styles for cards, buttons, input fields to implement  design.
-   ****:
    -   **Importance**: Global CSS styles.
    -   **Changes**: Modified to embed Montserrat font and apply global design elements.
</code_architecture>

<pending_tasks>
-   Implement counterparty editing functionality in the frontend within the counterparty detail card.
-   Handle Google Sheets API quota issues, potentially by implementing retry logic or suggesting user to increase quota.
-   Fully integrate the custom contract template (with alignment markers and other formatting) into the backend PDF generation process.
-   Extend template editing functionality to other document types (invoices, acts, waybills).
</pending_tasks>

<current_work>
Immediately before this summary request, the AI engineer was addressing the user's request to introduce a customizable contract template. A new file, , was created to handle the updated contract generation logic based on a detailed template provided by the user. The  was updated to utilize this new service and to accept a  field in the  Pydantic model.

On the frontend (), a Template Editor Dialog was implemented within the contracts section. This dialog allows users to edit the contract template text, with initial support for basic formatting (bold, italic, underline, font size) and specific alignment markers for per-paragraph alignment. State variables (, ) and corresponding functions (, , , ) were added to manage the template content and formatting settings. The last interaction involved making sure the  backend endpoint for contract generation is set up to receive this custom template from the frontend.
</current_work>

<optional_next_step>
Update the contract generation logic in  to use the custom template and its formatting settings received from the frontend.
</optional_next_step>
