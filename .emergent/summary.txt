<analysis>
The previous AI engineer focused on extending document generation capabilities, primarily replicating Acts functionality for Invoices, then fixing various issues. Initial work involved backend services, HTML templates, and frontend state/UI for invoices. Key challenges included fixing a frontend  syntax error causing , correcting invoice form data submission to the backend, unifying the invoice UI with existing Acts contract selection, and resolving issues where order PDF generation failed to display items. The latter was traced to incorrect Handlebars usage in the template and a regex issue in the backend's templating engine. The most recent major task involved refactoring the document saving logic to store data without generating PDFs initially, with PDF generation becoming an on-demand action. This refactor is partially complete for orders on the backend.
</analysis>

<product_requirements>
The application is a business document management system generating Invoices, Acts, and Contracts with HTML/CSS based PDF generation, preview, email, download, and Google Drive integration, supporting Ukrainian. The UI has a blue theme, Montserrat font, and rounded cards.

Recent focus included multi-step UI, multiple order selection, accurate PDF preview with Ukrainian word totals, template consistency, and correct  population.

The current overarching task is to implement Invoices (Рахунки) functionality mirroring Acts. This involves replicating the multi-step UI, allowing creation based on an order or without an order using a user-provided HTML template. This template requires iterative adjustments for layout, content (e.g., підстави block, Всього capitalization, Підпис:/М.П., payment details, alignment, table centering). The system also needs to resolve issues with creating invoices without orders (fixed an  error and payload mismatch) and later, a critical change: **documents (orders, invoices, acts, contracts) should save all data but NOT PDF files. PDF generation should be a separate, on-demand action.**
</product_requirements>

<key_technical_concepts>
-   **Full-stack Development**: React (frontend) and FastAPI (backend).
-   **PDF Generation**:  for HTML/CSS to PDF;  for Handlebars.
-   **State Management**: React Hooks ().
-   **API Communication**: HTTP requests for data and PDF blobs.
-   **Templating**: HTML templates (),  for versioning.
-   **Localization**: Ukrainian language support, number-to-words conversion.
-   **Data Storage**: Google Sheets for document data (orders, invoices, acts).
</key_technical_concepts>

<code_architecture>


-   ****:
    -   **Importance**: Central API router for the backend.
    -   **Changes**:
        -   Added new API endpoints for invoices (, ).
        -   Defined Pydantic models (, , ).
        -   **Latest changes**: Refactored  to only generate PDF from existing data. Added  for saving order data without PDF generation.
-   ****:
    -   **Importance**: Business logic for invoice generation.
    -   **Changes**: Initial creation, similar to , including , , and .
-   ****:
    -   **Importance**: HTML template for invoice PDF generation.
    -   **Changes**: Iterative adjustments based on user feedback, including layout, text formatting, , capitalization, adding Підпис: and М.П., refining payment details, and text alignment.
-   ****:
    -   **Importance**: Business logic for order generation.
    -   **Changes**:
        -   **Critical fix**: The  method's regex was updated from  to  to correctly handle both  and .
        -   Added logic for handling optional  to update existing orders.
-   ****:
    -   **Importance**: HTML template for order PDF generation.
    -   **Changes**: Updated to use  instead of  to correctly display pre-rendered HTML item rows.
-   ****:
    -   **Importance**: Handles interaction with Google Sheets for data storage and retrieval.
    -   **Changes**: Debugging and verification of  method to ensure orders are persistently saved. Implied fix for a duplicate function name  identified by the troubleshoot agent.
-   ****:
    -   **Importance**: Main React component managing state and UI.
    -   **Changes**:
        -   Added  for invoice-related state.
        -   Implemented functions for both from orders and without orders invoice creation (, , , etc.).
        -   **Critical fix**: Corrected structural error where  and related functions were placed incorrectly, causing .
        -   Refactored Invoices tab UI to a multi-step form and added template editor.
        -   **Fix for invoice without orders**: Modified  to calculate  and send correct payload, excluding  and  from the API request.
        -   **UI unification**: Modified invoice creation forms to consistently show a contract dropdown, including Без договору (Without contract) option, mirroring the Acts functionality for both based on order and without order scenarios.
        -   Updated logic for Generate PDF button in  to correctly call the  endpoint with current order details.
</code_architecture>

<pending_tasks>
-   **Frontend update for document saving**: Implement the new frontend logic to save document data (orders, invoices, acts, contracts) without immediately generating PDF files.
-   **Backend extension for document saving**: Extend the save without PDF and generate PDF on demand logic to Invoices and Acts.
-   **Thorough testing**: Conduct comprehensive frontend and backend testing for all document types with the new saving/PDF generation workflow.
</pending_tasks>

<current_work>
Immediately prior to this summary, the AI engineer was implementing a major architectural change: to save document data (like orders) without automatically generating PDF files, making PDF generation an on-demand action.

Specifically, for **Orders**, the backend () has been modified:
1.  A new endpoint  was added to save order data to Google Sheets without generating a PDF.
2.  The existing  endpoint was updated to solely generate a PDF based on *already saved* order data (retrieved from Google Sheets) or newly provided data if an  is not supplied.

During this implementation, an issue was encountered where newly created orders were not being persistently saved, or retrieved correctly, leading to Замовлення не знайдено errors. This was traced back to an underlying issue with the  function (previously identified by troubleshoot agent as a duplicate function name). After an implied fix (or re-diagnosis of the original fix being incomplete), the AI successfully created an order (e.g., order 0045) without a PDF and subsequently generated a PDF for this existing order. Debugging also involved adding  statements to  to confirm data flow.
</current_work>

<optional_next_step>
The next step is to update the frontend to use the new  endpoint for saving orders.
</optional_next_step>
